# 02 Model Comparison

The final `02_model_comparison.png` should show the complete 5-fold cross-validation table with accuracy and F1 scores, the best model at the top, and the model-comparison bar chart; delete this placeholder when that PNG is added.
