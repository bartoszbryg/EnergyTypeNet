# 05 Custom Dataset Analysis

The final `05_custom_dataset_analysis.png` should show an uploaded Iris dataset's profile, column configuration, baseline results table, and feature-ranking chart; delete this placeholder when that PNG is added.
