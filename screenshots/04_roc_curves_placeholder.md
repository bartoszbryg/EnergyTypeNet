# 04 ROC Curves

The final `04_roc_curves.png` should show ROC curves for all three building-type classes across the models with AUC values and legends visible; delete this placeholder when that PNG is added.
